import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Primary brand color (updated scheme ~ #6C5CE7)
  static const Color primary = Color(0xFF6C5CE7);

  // Common colors if needed
  static const Color white = Colors.white;
  static const Color grey = Color(0xFF9EA3AE);
  static const Color darkText = Color(0xFF101010);

  // Background
  static const Color background = Color(0xFFFAF9FF); // off-white/cream tint

  // Status soft backgrounds
  static const Color statusDoneBg = Color(0xFFECE6FF); // light purple
  static const Color statusInProgressBg = Color(0xFFFFF2E0); // light orange
  static const Color statusTodoBg = Color(0xFFE6F2FF); // light blue

  // Category circle colors
  static const Color categoryPink = Color(0xFFFF8AB8);
  static const Color categoryPurple = Color(0xFFB388FF);
  static const Color categoryOrange = Color(0xFFFFB074);
  static const Color categoryTeal = Color(0xFF66D1C1);
  static const Color categoryGrey = Color(0xFFB0B5C0);
}
